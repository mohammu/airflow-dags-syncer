categories:
- image
- config
- volumes
questions:
- variable:
  description: "Location of Docker image to use in Deployment"
  default: "my-repo"
  type: string
  required: true
  label: Docker image repo
